# modular-arithmetic-grokking
Exploring grokking phenomena in small transformers trained on modular arithmetic (p=113). Implements curriculum learning to avoid linear traps, ALiBi instead of learned positional embeddings, weight tying, and detailed embedding space analysis (PCA visualizations).
